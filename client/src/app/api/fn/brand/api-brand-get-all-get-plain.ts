/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BrandListDto } from '../../models/brand-list-dto';

export interface ApiBrandGetAllGet$Plain$Params {
}

export function apiBrandGetAllGet$Plain(http: HttpClient, rootUrl: string, params?: ApiBrandGetAllGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<BrandListDto>>> {
  const rb = new RequestBuilder(rootUrl, apiBrandGetAllGet$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<BrandListDto>>;
    })
  );
}

apiBrandGetAllGet$Plain.PATH = '/api/Brand/getAll';
