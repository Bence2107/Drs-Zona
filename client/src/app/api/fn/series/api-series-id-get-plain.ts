/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SeriesDetailDto } from '../../models/series-detail-dto';

export interface ApiSeriesIdGet$Plain$Params {
  id: string;
}

export function apiSeriesIdGet$Plain(http: HttpClient, rootUrl: string, params: ApiSeriesIdGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<SeriesDetailDto>> {
  const rb = new RequestBuilder(rootUrl, apiSeriesIdGet$Plain.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SeriesDetailDto>;
    })
  );
}

apiSeriesIdGet$Plain.PATH = '/api/Series/{id}';
